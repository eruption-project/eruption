
use builtin;
use str;

set edit:completion:arg-completer[magma] = {|@words|
    fn spaces {|n|
        builtin:repeat $n ' ' | str:join ''
    }
    fn cand {|text desc|
        edit:complex-candidate $text &display=$text' '(spaces (- 14 (wcswidth $text)))$desc
    }
    var command = 'magma'
    for word $words[1..-1] {
        if (str:has-prefix $word '-') {
            break
        }
        set command = $command';'$word
    }
    var completions = [
        &'magma'= {
            cand -c 'Sets the configuration file to use'
            cand --config 'Sets the configuration file to use'
            cand -v 'Verbose mode (-v, -vv, -vvv, etc.)'
            cand --verbose 'Verbose mode (-v, -vv, -vvv, etc.)'
            cand -h 'Print help'
            cand --help 'Print help'
            cand -V 'Print version'
            cand --version 'Print version'
            cand completions 'Generate shell completions'
            cand manpages 'Generate manual pages'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'magma;completions'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'magma;manpages'= {
            cand -h 'Print help'
            cand --help 'Print help'
        }
        &'magma;help'= {
            cand completions 'Generate shell completions'
            cand manpages 'Generate manual pages'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'magma;help;completions'= {
        }
        &'magma;help;manpages'= {
        }
        &'magma;help;help'= {
        }
    ]
    $completions[$command]
}
